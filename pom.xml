<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.study.nuwa</groupId>
    <artifactId>nuwa-cloud</artifactId>
    <packaging>pom</packaging>
    <version>1.0-SNAPSHOT</version>
    <modules>
        <module>nuwa-common</module>
        <module>nuwa-gateway</module>
        <module>nuwa-plugin</module>
        <module>nuwa-service</module>
        <module>nuwa-service-client</module>
        <!--
            nuwa-oauth2 临时下线：Spring Boot 3.x 已移除 Spring Security OAuth 项目
            (spring-security-oauth2)，全部代码基于老版本 API 编写（AuthorizationServerConfigurerAdapter、
            ResourceServerConfigurerAdapter、JdbcClientDetailsService、JwtAccessTokenConverter 等），
            迁移目标为 Spring Authorization Server (spring-security-oauth2-authorization-server)，
            工作量较大，需独立 PR。暂时从构建中摘掉。
        -->
        <!--        <module>nuwa-oauth2</module>-->
    </modules>

    <properties>
        <!-- jdk版本1.8 -->
        <java.version>17</java.version>
        <!-- maven-compiler-plugin插件版本，Java代码编译 -->
        <maven.plugin.version>3.11.0</maven.plugin.version>
        <!-- maven编译时指定编码UTF-8 -->
        <maven.compiler.encoding>UTF-8</maven.compiler.encoding>
        <!-- 项目统一字符集编码UTF-8 -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- 项目统一字符集编码UTF-8 -->
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <!-- nuwa项目统一设置版本号 -->
        <nuwa.project.version>1.0-SNAPSHOT</nuwa.project.version>
        <!-- SpringBoot版本号 -->
        <spring.boot.version>3.3.5</spring.boot.version>
        <!-- SpringCloud版本号 -->
        <spring.cloud.version>2023.0.3</spring.cloud.version>
        <!-- SpringPlatform版本号 -->
        <spring.platform.version>Cairo-SR8</spring.platform.version>
        <!-- mysql数据库驱动 -->
        <mysql.connector.version>8.4.0</mysql.connector.version>
        <!-- 数据库连接池Druid -->
        <druid.version>1.2.23</druid.version>
        <!-- Mybatis Plus增强工具 -->
        <mybatis.plus.version>3.5.9</mybatis.plus.version>
        <!-- Lombok 版本：JDK17 需要 1.18.30+，推荐 1.18.48 解决 1.18.34 在某些 JDK17 版本的注解处理器不触发问题 -->
        <lombok.version>1.18.48</lombok.version>
    </properties>

    <dependencies>
        <!-- Lombok 通过简单注解消除冗长代码  -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!-- spring boot工程的依赖库,无需继承spring-boot-starter-parent -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- spring cloud工程的依赖库 -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- nuwa-Component相关Jar包 -->
            <dependency>
                <groupId>com.study.nuwa</groupId>
                <artifactId>nuwa-platform-bom</artifactId>
                <version>${nuwa.project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>


    <build>
        <finalName>${project.name}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*.xml</include>
                </includes>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <!-- 能够将Spring Boot应用打包为可执行的jar或war文件，然后以通常的方式运行Spring Boot应用 -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                    <configuration>
                        <fork>true</fork>
                        <finalName>${project.build.finalName}</finalName>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>16</source>
                    <target>16</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>